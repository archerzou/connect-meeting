# Intentionally minimal. The base docker-compose.yml is self-contained (config via
# environment variables), so the same stack is portable to Dokploy/production
# without Visual Studio-only user-secret and HTTPS cert mounts.
services: {}
